# THIS FILE IS MANAGED BY PUPPET
# Full config example: https://gitweb.torproject.org/tor.git/plain/src/config/torrc.sample.in

<% if @log_level %>
Log <%= @log_level %> stdout
<% end %>

<% if @nickname %>
Nickname <%= @nickname %>
<% end %>

<% if @contactinfo -%>
ContactInfo <%= @contactinfo %>
<% end -%>

################ This section is just for relays #####################
## See https://www.torproject.org/docs/tor-doc-relay for details.

<% if @orport %>
ORPort <%= @orport %>
<% end %>

## Define these to limit how much relayed traffic you will allow. Your
## own traffic is still unthrottled. Note that RelayBandwidthRate must
## be at least 20 kilobytes per second.
## Note that units for these config options are bytes (per second), not
## bits (per second), and that prefixes are binary prefixes, i.e. 2^10,
## 2^20, etc.
#RelayBandwidthRate 500 KBytes  # Throttle traffic to 100KB/s (800Kbps)
#RelayBandwidthBurst 750 KBytes # But allow bursts up to 200KB (1600Kb)

<% @exitpolicy.each do |exitpolicyarray| -%>
ExitPolicy <%= exitpolicyarray %>
<% end -%>

